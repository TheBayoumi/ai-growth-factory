# Deployment status

The source repository is production-ready, but the Modal GPU worker and YouTube publisher remain undeployed until account-owned credentials and approvals are supplied.

No credential, model weight, generated media, or private OAuth token is stored in this repository.
