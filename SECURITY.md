# Security policy

Do not report credentials in public issues. Revoke exposed YouTube, Modal, Hugging Face or OpenAI credentials immediately.

The repository must never contain OAuth refresh tokens, API keys, generated production media, model caches or downloaded model weights. Use Modal secrets and protected GitHub environments.
