# Modal GPU production verification

Trigger: `20260801-02`

This merge verifies the persistent GitHub-secret authentication path and runs the real render-only Modal T4 canary against the current `main` source. Publishing remains disabled. The run must export the exact MP4, WAV, voice-review manifest, package, and video-QC report before it can pass.
