# Modal GPU production verification 2026-08-02-01

Trigger the protected merge-only Modal T4 render canary against CI-green main commit `3f4f88b5f099512f758faca248ee889b66de5686`.

This run uses persistent Modal credentials and verifies the completed Qwen Omni multimodal runtime during image construction before T4 inference. Publishing remains disabled. Success requires `verified_render_canary` plus the exact MP4, narration WAV, source package, voice-review manifest, and video-QC report. The exported media must be inspected before production approval.
