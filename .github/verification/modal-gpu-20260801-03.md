# Modal GPU production verification

Trigger: `20260801-03`

This merge reruns the real render-only Modal T4 production canary against the exact current `main` source after the persistent-credential fail-fast fix. Publishing remains disabled. Passing requires successful merge-only CI plus the exact MP4, WAV, package, voice-review manifest, and video-QC report with status `verified_render_canary`.
