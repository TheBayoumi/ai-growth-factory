# Modal GPU production verification 2026-08-02 / 09

Trigger the protected merge-only workflow against current `main`.

Acceptance order:

1. validate persistent `modal-production` credentials without browser authorization;
2. deploy the current Modal worker;
3. run the real T4 reviewer import probe and capture exact package/CUDA diagnostics;
4. run the full research, script, Qwen3-TTS, Qwen Omni review, render, and video-QC canary only after the probe passes;
5. export the exact media and QC bundle for perceptual inspection.

Publishing remains disabled.
