# Modal GPU production verification 2026-08-02 / 10

Trigger the current protected workflow after the concurrency isolation fix.

This run must:

1. use persistent `modal-production` credentials with no browser authorization;
2. execute under `modal-gpu-production-verification-v2`;
3. pass the real T4 Qwen3-TTS/Qwen Omni import probe;
4. run the complete render-only production canary;
5. export the exact MP4, WAV, manifests, package, and video-QC report for inspection.

Publishing remains disabled.
